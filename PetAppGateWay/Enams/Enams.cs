namespace PetAppGateWay.Enams
{
    public enum Roles : byte { Admin, Person}
    public enum Claims : byte { Id } 
    public enum CashTag : byte { GetUser }
    public enum CashPolicy : byte { GetUser }
    public enum RadisKeys : byte { Currency }
}
